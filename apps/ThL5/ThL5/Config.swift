import SwiftUI

// この級のためにビルド時に生成される設定（アプリ本体のコードは全検定で共通）

let APP_TITLE = "タイ語模擬問題集"
let LEVEL_DISPLAY = "5級"
let LEVEL_KEY = "5"
let DATA_NAME = "exam_data"
let RECORD_KEY = "thaiRecords.v1"

// 読み上げ（iOS 標準の音声合成のみ）
let SPEECH_LANG = "th-TH"
let SPEECH_PREFIX = "th"
let SCRIPT = "thai"
let CONTENT_FONT = ""   // 学習言語の本文フォント（空＝端末標準）

// 選択肢の記号（本試験の表記に合わせる）
let MARK_STYLE = "circled"
// 大問共通の選択肢（対応づけ・段落挿入）の記号。空なら MARK_STYLE と同じ
let MARK_STYLE_SHARED = ""

// 表示テキスト
let DISCLAIMER_HOME = "本アプリは個人が独自に作成した学習用の模擬問題集です。実用タイ語検定試験を実施する団体とは一切関係がなく、公認・提携・推薦を受けたものではありません。収録した問題・解説はすべて本アプリのために新規に作成したもので、実際の試験問題を転載したものではありません。"
let ABOUT_SECTIONS: [(String, String)] = [
    ("このアプリについて", "タイ語模擬問題集は、タイ語学習者のための模擬問題集アプリです。全5回の模擬試験を収録し、学習モードでは1問ずつ解説を、本番モードでは制限時間つきの通し演習と採点ができます。"),
    ("公式試験との関係", "本アプリは個人が独自に制作した非公式の学習教材です。実用タイ語検定試験およびその実施団体とは一切関係がなく、公認・提携・監修・推薦を受けたものではありません。出題形式は公開されている一般的な情報を参考にした独自解釈であり、実際の試験の構成・難易度・配点を保証するものではありません。"),
    ("配点・合格点について", "本試験は筆記とリスニングの合計100点満点で70点以上が合格です（項目別の基準点はありません）。本アプリは筆記に特化しており、リスニング・二次試験は収録していません。得点の目安は、この基準（70%）を筆記の満点に当てた参考値です。"),
    ("問題・解説の著作権", "収録されているすべての問題文・選択肢・和訳・解答例・解説は、本アプリのために新規に作成したオリジナルです。読解の本文も架空の内容として書き下ろしたもので、過去問題その他の第三者の著作物を転載・翻案したものではありません。これらの著作権は制作者に帰属します。"),
    ("音声について", "5級は本試験と同じくタイ文字を使わず、ローマ字とカタカナの発音表記で出題するため、読み上げ機能はありません。4級以上では、iOS 標準の音声合成機能のみでタイ語を読み上げます（音声データは同梱していません）。"),
    ("プライバシー", "本アプリは個人情報を一切収集しません。学習履歴と採点結果は端末内にのみ保存され、外部に送信されることはありません。通信機能・広告・解析ツールを使用していません。"),
    ("商標について", "文中の団体名・試験名は各権利者の商標または登録商標です。本アプリでは、対応する学習範囲を説明する目的でのみ言及しています。")
]

// 配色
let FLAG_VERTICAL_STRIPES = false
let FLAG_COLORS: [Color] = [Color(red: 0.647, green: 0.098, blue: 0.192), Color(red: 0.957, green: 0.961, blue: 0.973), Color(red: 0.176, green: 0.165, blue: 0.290), Color(red: 0.957, green: 0.961, blue: 0.973), Color(red: 0.647, green: 0.098, blue: 0.192)]

extension Color {
    static let exAccent = Color(red: 0.176, green: 0.165, blue: 0.290)
    static let exRed = Color(red: 0.647, green: 0.098, blue: 0.192)
    static let exGold = Color(red: 0.784, green: 0.604, blue: 0.173)
}
